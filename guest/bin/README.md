# Guest Utilities

This directory contains the source launchers installed by the Python stage:
`mtaw-shell` activates the per-user MTAW virtual environment in an interactive
shell, and `mtaw-jupyter` starts JupyterLab bound to loopback without opening a
browser. They contain no credentials or browser-profile configuration.
